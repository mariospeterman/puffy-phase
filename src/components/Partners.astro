---
const partners = [
  {
    name: "MetaMask",
    type: "wallet",
    logo: "/partners/metamask.svg"
  },
  {
    name: "WalletConnect",
    type: "wallet",
    logo: "/partners/walletconnect.svg"
  },
  {
    name: "Gnosis Safe",
    type: "service",
    logo: "/partners/gnosis.svg"
  },
  {
    name: "The Graph",
    type: "service",
    logo: "/partners/thegraph.svg"
  },
  {
    name: "Chainlink",
    type: "service",
    logo: "/partners/chainlink.svg"
  },
  {
    name: "OpenZeppelin",
    type: "service",
    logo: "/partners/openzeppelin.svg"
  }
];

// Group partners by type
const wallets = partners.filter(p => p.type === "wallet");
const services = partners.filter(p => p.type === "service");
---

<section class="py-16 md:py-24 relative overflow-hidden">
  <!-- Background Elements with ENS-like colored blocks -->
  <div class="absolute inset-0 bg-gray-50 dark:bg-gray-900 -z-10"></div>
  <div class="absolute top-20 left-20 w-[150px] h-[90px] bg-green-500 rounded-lg opacity-30 -rotate-6 z-0"></div>
  <div class="absolute bottom-40 right-[10%] w-[200px] h-[100px] bg-amber-400 rounded-lg opacity-30 rotate-12 z-0"></div>
  
  <div class="container mx-auto px-4">
    <div class="max-w-3xl mx-auto text-center mb-12">
      <span class="inline-block px-3 py-1 bg-purple-100 dark:bg-purple-900/40 text-purple-600 dark:text-purple-400 text-sm font-medium rounded-full mb-3">Infrastructure</span>
      <h2 class="text-3xl md:text-4xl font-bold mb-4 bg-gradient-to-r from-purple-600 to-blue-500 bg-clip-text text-transparent">Key Partners</h2>
      <p class="text-lg text-gray-700 dark:text-gray-300 max-w-2xl mx-auto">
        These partners are revolutionizing retirement alongside BTC DAO, creating secure infrastructure for a better financial future.
      </p>
    </div>

    <!-- Partner categories -->
    <div class="max-w-4xl mx-auto mb-10">
      <div class="flex flex-col gap-8">
        <!-- Wallets Section -->
        <div>
          <h3 class="text-xl font-semibold text-center mb-6 text-transparent bg-gradient-to-r from-purple-600 to-blue-500 bg-clip-text">Wallet Integrations</h3>
          <div class="flex flex-wrap justify-center items-center gap-8 md:gap-12">
            {wallets.map(partner => (
              <div class="w-32 h-24 flex items-center justify-center grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300">
                <div class="flex flex-col items-center">
                  <div class="w-14 h-14 flex items-center justify-center bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-xl shadow-sm mb-2 border border-gray-100 dark:border-gray-700 hover:border-purple-200 dark:hover:border-purple-700 transition-colors">
                    <img src={partner.logo} alt={partner.name} class="w-10 h-10" loading="lazy" />
                  </div>
                  <span class="text-xs text-center text-gray-700 dark:text-gray-300">{partner.name}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
        
        <!-- Services Section -->
        <div>
          <h3 class="text-xl font-semibold text-center mb-6 text-transparent bg-gradient-to-r from-purple-600 to-blue-500 bg-clip-text">Service Providers</h3>
          <div class="flex flex-wrap justify-center items-center gap-8 md:gap-12">
            {services.map(partner => (
              <div class="w-32 h-24 flex items-center justify-center grayscale opacity-70 hover:grayscale-0 hover:opacity-100 transition-all duration-300">
                <div class="flex flex-col items-center">
                  <div class="w-14 h-14 flex items-center justify-center bg-white/90 dark:bg-gray-800/90 backdrop-blur-sm rounded-xl shadow-sm mb-2 border border-gray-100 dark:border-gray-700 hover:border-purple-200 dark:hover:border-purple-700 transition-colors">
                    <img src={partner.logo} alt={partner.name} class="w-10 h-10" loading="lazy" />
                  </div>
                  <span class="text-xs text-center text-gray-700 dark:text-gray-300">{partner.name}</span>
                </div>
              </div>
            ))}
          </div>
        </div>
      </div>
    </div>
    
    <!-- Domain-like blocks -->
    <div class="flex flex-wrap justify-center gap-4 mt-8">
      <div class="bg-purple-100 py-2 px-5 rounded-lg text-purple-800 font-medium">
        ecosystem.partners
      </div>
      <div class="bg-blue-100 py-2 px-5 rounded-lg text-blue-800 font-medium">
        security.providers
      </div>
      <div class="bg-green-100 py-2 px-5 rounded-lg text-green-800 font-medium">
        wallet.connect
      </div>
    </div>
  </div>
</section> 